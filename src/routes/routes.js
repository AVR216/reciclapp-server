const auth = require('./auth');
const donations = require('./donations');
const employees = require('./employees');
const users = require('./users');
const collects = require('./collect');

module.exports = {
    auth,
    donations,
    employees,
    users,
    collects
};